version: '3'

services:
  # Database
  db:
    container_name: mysql-sequelize
    image: mysql:8.0.23
    restart: always
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: root
      # MYSQL_DATABASE: mysql 
      # MYSQL_USER: mysql
      # MYSQL_PASSWORD: mysql
